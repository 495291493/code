package com.clschina.common.test;

import junit.framework.TestCase;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;

import com.clschina.common.util.Encryption;



public class EncryptionTestCase extends TestCase {
	private static Log log = LogFactory.getLog(EncryptionTestCase.class);

	public void testSendMail()  {
//		try{
//			log.trace("test Encryption ");
//	
//			String source = "hello 世界.";
//			String target = Encryption.encodeString(source);
//			String s2 = Encryption.decodeString(target);
//			log.trace(source + " -------> " + target + " ---> " + s2);
////			assertEquals(source, s2);
//		}catch(Exception e) {
//			log.error("", e);
//		}
	}
}
